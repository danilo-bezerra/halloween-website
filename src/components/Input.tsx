import styled from "styled-components";

export const Input = styled.input`
  height: 3rem;
  padding: 0.75rem;
  border: none;
  outline: none;
  font-weight: bolder;
  font-size: 14px;
  border-radius: 0.25rem;
`;
